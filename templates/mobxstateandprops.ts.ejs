export interface <%= controller.name %>Props{

}

export interface <%= controller.name %>State{

}

export const <%= controller.name %>StateKey = '<%= controller.name %>ID'; 

export const <%= controller.name %>InitialState:<%= controller.name %>State = {
    
}