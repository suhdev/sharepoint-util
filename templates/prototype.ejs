<!DOCTYPE html>
<html lang="en">
    <head>
        <title><%= name %> Prototype</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
        <link href="../css/main.min.css" rel="stylesheet">
        <link href="../css/<%= baseName %>.css" rel="stylesheet">
        <script type="text/javascript" src="../js/vendor.js"></script>
    </head>
    <body>
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h1 class="display-3">{{definition.name | replace(r/([a-z])([A-Z])/g,'$1 $2')}}</h1>
                <p class="lead">{{definition.description}}</p>
                <p class="lead">
                    <a class="btn btn-primary btn-lg" href="../" role="button">Home</a>
                </p>
            </div>
        </div>
        <div class="container">
            <div id="Prototype"></div>
        </div>
        <div class="container">
            {% if definition.todos %}
            <br/>
            <h2>{{definition.title}} Todo List</h2>
            <p class="lead">Here is a list of the todos for {{definition.name | replace(r/([a-z])([A-Z])/g,'$1 $2')}}</p>
            {% for todo in definition.todos %}
                <a class="list-group-item list-group-item-action">
                    <div class="d-flex w-100 justify-content-between">
                        <h5 class="mb-1">{{todo.content}} 
                        {% if todo.status == 'Done' %}
                        <span class="badge badge-success badge-pill">Done</span>
                        {% elif todo.status == 'In Progress' %}
                        <span class="badge badge-warning badge-pill">In Progress</span>
                        {% else %}
                        <span class="badge badge-warning badge-pill">Pending</span>
                        {% endif %}</h5>
                        {% if todo.jiraIssue %}
                        <small>{{todo.jiraIssue.key}}</small> 
                        {% endif %}
                    </div>
                    <p class="mb-1">{{todo.content}}</p>
                    {% if todo.jiraIssue %}<small class="text-muted">{{todo.jiraIssue.fields.summary}}</small>{% endif %}
                    
                </a>
            {% endfor %}
            {% endif %}
        </div>
        <script type="text/javascript" src="../js/<%= baseName %>.js"></script>
    </body>
</html>